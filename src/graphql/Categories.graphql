query GetCategories {
    categories {
        id
        name
        subcategoriesIds
        image
    }
}

mutation DeleteCategory($id: String!) {
    deleteCategory(input: {id: $id}) {
        deleted
    }
}

mutation UpdateCategory($id: String!, $name: String!) {
    updateCategory(input: {id: $id, name: $name}) {
        updated
    }
}

mutation CreateNewCategory($name: String!, $image: Upload!) {
    createCategory(input: {name: $name, image: $image}) {
        createdCategory {
            id
        }
    }
}