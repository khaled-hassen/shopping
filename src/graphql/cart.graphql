mutation Checkout {
    checkout {
        redirectUrl

        errors {
            ...on EmptyCartError {
                message
            }

            ...on EmptyBillingDetailsError {
                message
            }
        }
    }
}